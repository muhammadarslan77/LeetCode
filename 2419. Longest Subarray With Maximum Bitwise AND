class Solution:
    def longestSubarray(self, nums: List[int]) -> int:
        ans, count = 0, 0
        
        max_element = max(nums)
        for num in nums:
            if num == max_element:
                count += 1
            else:
                count = 0
            ans = max(ans, count)
        return ans
