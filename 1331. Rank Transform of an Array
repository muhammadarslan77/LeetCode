class Solution:
    def arrayRankTransform(self, arr: List[int]) -> List[int]:
        a = enumerate(sorted(set(arr)))
        d = {n:i+1 for i,n in a}

        for i in range(len(arr)):
            arr[i] = d[arr[i]]

        return arr
